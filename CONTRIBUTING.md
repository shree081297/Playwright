Thank you for contributing!

Guidelines:

- Fork the repository and open a pull request.
- Run tests locally before submitting: `npm test`.
- Follow code style: run `npm run lint` and `npm run format`.
- Do not commit secrets—use `.env` for local variables.

For major changes, open an issue first to discuss scope.
